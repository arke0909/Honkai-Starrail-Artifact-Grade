# 붕괴: 스타레일 유물 점수 계산기 개발 계획서

## 1. 문서 목적

이 문서는 프로젝트의 개발 계획, 구현 과정, 사용법, 테스트 결과, 문제 해결 과정, 실사용 후기를 한곳에 누적하기 위한 기준 문서다. 앞으로 기능을 추가하거나 수정할 때마다 작업 내용과 검증 결과를 이 문서에 기록한다.

## 2. 프로젝트 개요

- 주제: 붕괴: 스타레일 유물(성유물) 점수 계산기
- 목표: 사용자가 UID 또는 Scanner JSON으로 유물을 불러오면 장착 캐릭터의 실제 성장 방식에 맞는 점수, 등급, 주옵션과 12개 부옵션 유효도를 즉시 확인할 수 있는 웹 앱을 만든다.
- 대상 사용자: 보유 유물의 강화 가치와 캐릭터별 적합도를 빠르게 비교하고 싶은 플레이어
- 사용 기술: C#, .NET 10, Blazor WebAssembly, ASP.NET Core Web API, Redis, xUnit, Docker Compose
- 배포 형태: 설치 없이 브라우저에서 사용하는 웹 앱. Blazor 클라이언트와 ASP.NET Core API를 배포하고, API가 Redis에 최근 계산 기록과 사용 통계를 저장한다.

## 3. 핵심 개발 원칙

1. 계산식과 화면 표시 코드를 분리한다.
2. 스탯 이름, 가중치, 등급 기준은 데이터에서 관리하고 화면 코드에 중복 저장하지 않는다.
3. 사용자가 점수의 근거를 확인할 수 있도록 스탯별 기여도를 함께 보여준다.
4. 잘못된 입력은 조용히 무시하지 않고 입력 위치와 해결 방법을 표시한다.
5. 현재 사용자 흐름은 자동 가져오기와 캐릭터 탭 선택에 집중하고, 중복되는 수동 입력 화면은 두지 않는다.
6. 모든 작업은 코드 변경, 검증 결과, 문제와 해결 방법까지 이 문서에 기록한 뒤 완료로 처리한다.

## 4. 구현 범위

### 4.1 필수 기능(MVP)

- UID 공개 프로필 또는 Scanner JSON에서 5성 유물과 장착 캐릭터를 가져온다.
- 선택한 캐릭터 ID의 부위별 주옵션과 12개 부옵션 가중치를 자동으로 적용한다.
- HP·공격력·방어력 기반 치명타, 격파, 효과 명중, 지원 유형을 실제 개별 가중치에서 요약한다.
- 모든 옵션을 핵심·유효·보조·비유효로 표시하고 유물별 점수 기여도를 함께 보여준다.
- 미등록 캐릭터와 미장착 유물에는 잘못된 범용 점수 대신 평가 불가 이유를 표시한다.
- 모바일과 데스크톱에서 모두 사용할 수 있는 반응형 화면을 제공한다.

### 4.2 완성도를 높이는 후속 기능

- 여러 유물의 점수 비교와 정렬
- 결과를 이미지 또는 텍스트로 공유하는 기능
- 다크 모드와 키보드 접근성 개선
- 사용자 피드백을 반영한 점수 기준 보정

후속 기능은 MVP의 계산 정확성, 테스트, 배포가 완료된 뒤 우선순위를 다시 판단한다.

## 5. 점수 계산 설계

점수는 자동으로 불러온 주·부옵션의 수치와 캐릭터 ID별 가중치를 사용해 계산하며, 100점 기준에서 주옵션과 부옵션에 각각 50%를 배정한다.

```text
주옵션 점수 = (유물 레벨 + 1) / 16 × 주옵션 가중치 × 50
부옵션 롤 단위 = 기본 롤 횟수 + 품질 상승 단계 수 × 0.1
부옵션 기여도 = 부옵션 롤 단위 × 부옵션 가중치 × (50 / 캐릭터 max)
총점 = 주옵션 점수 + 모든 부옵션 기여도의 합
등급 = 검증된 구간에 따라 S, A, B, C 등으로 표시
```

- 기준 수치와 가중치는 UI와 분리된 C# Domain 코드에서 한 번만 관리한다.
- 치명타 확률처럼 표시 단위가 퍼센트인 값과 속도처럼 고정 수치인 값을 구분한다.
- 주옵션은 부위별 캐릭터 가중치와 강화 레벨로 계산하고 카드에 적합도·가중치·기여도를 따로 표시한다.
- 정확한 기준 수치, 가중치, 등급 구간은 구현 전에 게임 데이터와 대표적인 계산 방식을 조사하고 샘플 유물로 검증한 뒤 확정한다.
- 결과 화면에는 총점만 보여주지 않고 캐릭터 유형, 12개 가중치, 유효도와 스탯별 기여도를 함께 표시한다.

## 6. 화면 구성

1. 헤더: 프로젝트 이름과 간단한 사용 설명
2. 자동 가져오기 영역: UID 입력 또는 Scanner JSON 선택
3. 캐릭터 탭: 장착 캐릭터별 유물 묶음 선택
4. 캐릭터 평가 영역: 유형 요약과 12개 옵션 가중치 전체
5. 결과 영역: 왼쪽 캐릭터 이미지, 오른쪽 유물 이미지·주옵션·부옵션 유효도·점수
6. 푸터: GitHub 저장소, 데이터 기준일

한 화면에서 자동 가져오기와 결과를 확인할 수 있게 구성하며, 모바일에서는 캐릭터 이미지 아래에 유물 카드가 이어지도록 배치한다.

## 7. 예상 코드 구조

```text
src/
  ArtifactGrade.Domain/  점수 계산, 입력 검증, 스탯 기준
  ArtifactGrade.Api/     계산 API와 Redis 저장
  ArtifactGrade.Client/  Blazor WebAssembly UI
tests/
  ArtifactGrade.Domain.Tests/  계산 및 검증 테스트
docs/
  DEVELOPMENT_PLAN.md
README.md
docker-compose.yml
```

계산기는 UI에 의존하지 않는 순수 함수로 만들고, UI는 입력을 전달하고 결과를 표시하는 역할만 맡는다.

## 8. 단계별 개발 계획

### 1단계: 요구사항과 계산 기준 확정

- 게임 내 유물 부위, 주옵션, 부옵션, 강화 규칙을 정리한다.
- 평가 프로필별 유효 스탯과 가중치를 정한다.
- 샘플 유물과 예상 점수를 작성해 계산식 검증 기준으로 사용한다.
- 완료 조건: 같은 입력에 항상 같은 결과가 나오며 계산 과정을 손으로 재현할 수 있다.

### 2단계: C# 프로젝트 기반과 기본 화면 구현

- .NET 10, Blazor WebAssembly, ASP.NET Core 개발 환경을 구성한다.
- 입력 폼과 결과 카드의 반응형 화면을 구현한다.
- 접근 가능한 라벨, 키보드 입력, 명확한 오류 문구를 적용한다.
- 완료 조건: PC와 모바일 화면에서 모든 입력 항목을 불편 없이 사용할 수 있다.

### 3단계: 계산 및 검증 로직 구현

- 스탯 데이터, 프로필 가중치, 등급 기준을 정의한다.
- 점수 계산 순수 함수와 입력 검증을 구현한다.
- 스탯별 기여도와 총점이 일치하도록 결과를 연결한다.
- 완료 조건: 정상값, 경계값, 잘못된 값에 대한 자동 테스트를 통과한다.

### 4단계: 사용자 편의 기능 구현

- 마지막 입력과 설정을 브라우저에 저장한다.
- 초기화와 결과 공유 기능을 추가한다.
- Redis에 최근 계산 기록과 사용 통계를 저장하고, Redis 장애 시 계산 결과와 저장 실패 안내를 함께 반환한다.
- 빈 화면, 오류, 저장 데이터 손상 상황을 처리한다.
- 완료 조건: 새로고침과 재방문 후에도 정상 동작하고 잘못된 저장값은 사용자에게 알린다.

### 5단계: 품질 점검과 배포

- 단위 테스트, 사용자 흐름 테스트, 프로덕션 빌드를 실행한다.
- Chrome, Edge 및 모바일 화면 크기에서 직접 확인한다.
- GitHub Actions에서 테스트와 빌드를 자동화한다.
- Blazor 클라이언트, ASP.NET Core API, Redis를 지원하는 환경에 배포한다. GitHub Pages는 서버와 Redis를 실행할 수 없으므로 전체 서비스의 단독 배포 대상으로 사용하지 않는다.
- README에 소개, 설치법, 실행법, 계산 기준, 배포 주소를 정리한다.
- 완료 조건: 새 환경에서 문서만 보고 실행할 수 있고 공개 주소에서 오류 없이 동작한다.

### 6단계: 실사용과 공개

- 실제 보유 유물을 최소 10개 입력해 점수와 사용성을 확인한다.
- 장점, 단점, 불편한 점, 개선 우선순위를 실사용 후기로 작성한다.
- 스크린샷 또는 짧은 시연 자료와 함께 개발 블로그나 개인 SNS에 게시한다.
- GitHub 저장소의 설명, README, 라이선스, 배포 링크를 최종 점검한다.
- 완료 조건: 후기 및 게시물 링크가 문서와 README에 기록되어 있다.

## 9. 테스트 계획

- 계산 테스트: 각 스탯, 가중치 0, 최대·최소값, 반올림, 등급 경계
- 검증 테스트: 필수값 누락, 동일 부옵션 중복, 숫자 외 입력, 허용 범위 초과
- 상태 테스트: 저장, 복원, 초기화, 손상된 저장 데이터 처리
- UI 테스트: 입력 후 결과 변경, 오류 메시지, 모바일 배치, 키보드 조작
- 배포 테스트: 새 브라우저에서 공개 주소 접속, 새로고침, 정적 자원 로딩
- 회귀 테스트: 계산 기준 변경 후 기존 샘플 유물의 결과 비교

## 10. 문서화 규칙

모든 개발 작업은 아래 순서로 기록한다.

1. 작업 전: 목적, 범위, 완료 조건을 `작업 기록`에 추가한다.
2. 작업 중: 중요한 선택과 변경된 파일을 기록한다.
3. 작업 후: 실행한 테스트, 결과, 남은 문제를 기록한다.
4. 오류 해결 시: 증상, 원인, 해결 방법, 재발 방지 방법을 `트러블슈팅`에 추가한다.
5. 배포 또는 사용법이 달라지면 README와 이 문서를 같은 작업에서 갱신한다.

작업 기록 형식은 다음과 같다.

```text
날짜 / 작업명
- 목적:
- 변경 내용 및 파일:
- 결정과 이유:
- 검증 방법 및 결과:
- 문제와 해결:
- 다음 작업:
```

## 11. 평가 항목 대응표

| 평가 항목 | 확보 방법 | 현재 상태 |
|---|---|---|
| 1. 주제 선정 및 구현 | 유물 점수 계산 MVP 구현 | O - C# MVP 구현 완료 |
| 2. 높은 완성도 | 자동 테스트, 브라우저 점검, API·Redis 포함 정상 배포 | X - 로컬 검증 완료, 실제 Redis 연결·외부 배포 전 |
| 3. 개발 과정과 사용법 문서화 | 이 문서의 작업 기록·트러블슈팅과 README 유지 | O - 설치·실행·코드·트러블슈팅 작성 |
| 4. 직접 사용 및 후기 | 실제 유물 10개 이상 평가 후 장단점과 개선점 작성 | O - 공개 UID의 실제 유물 48개로 장단점·개선점 작성 |
| 5. 블로그 또는 SNS 게시 | 시연 자료와 배포 링크 게시 후 URL 기록 | X - 게시 전 |
| 6. GitHub 정리 및 업로드 | 저장소 구조, README, 라이선스, 배포 링크 정리 및 푸시 | O - 기능 브랜치와 `main` 업로드·CI 통과 |

현재 충족 점수는 16점이며, 최종 목표는 6개 항목 모두 충족하여 24점을 확보하는 것이다. 상태는 실제 결과와 증거를 확인한 경우에만 O로 변경한다.

## 12. 작업 기록

### 2026-09-13 / 초기 개발 계획 수립

- 목적: 빈 저장소에서 프로젝트 범위와 완료 기준을 명확히 정한다.
- 변경 내용 및 파일: `docs/DEVELOPMENT_PLAN.md`를 생성했다.
- 결정과 이유: 설치 장벽이 낮고 GitHub Pages 배포가 쉬운 React, TypeScript, Vite 기반 정적 웹 앱을 제안했다. 첫 버전은 계산의 정확성과 설명 가능성을 우선하여 수동 입력형으로 제한했다.
- 검증 방법 및 결과: 저장소에는 프로젝트 설명만 있는 `README.md` 외에 구현 파일이 없음을 확인했다. 계획 문서에 기능, 구조, 단계, 테스트, 평가 항목을 연결했다.
- 문제와 해결: 정확한 점수 기준은 아직 검증되지 않았으므로 임의의 수치를 확정하지 않고 1단계 조사와 샘플 검증 대상으로 남겼다.
- 다음 작업: 계산 기준과 MVP 요구사항을 확정한 뒤 프로젝트 기반을 구성한다.

### 2026-09-13 / 구현 언어 변경 및 Redis 도입 결정

- 목적: 사용자 요청에 맞춰 전체 구현 언어를 C#으로 통일하고 Redis 활용 범위를 정한다.
- 변경 내용 및 파일: 기술 구성을 C#, .NET 10, Blazor WebAssembly, ASP.NET Core Web API, Redis, xUnit으로 변경했다. 앞서 준비한 TypeScript 초기 설정과 테스트 파일은 실제 구현 전에 제거했다.
- 결정과 이유: 브라우저 UI는 Blazor WebAssembly로, 계산 및 저장은 ASP.NET Core API로 구현한다. Redis는 최근 계산 기록과 사용 통계를 저장한다.
- 검증 방법 및 결과: 로컬 환경에 .NET SDK 10.0.400이 설치되어 있고 Blazor, Web API, xUnit 템플릿을 사용할 수 있음을 확인했다.
- 문제와 해결: Redis는 정적 사이트에서 직접 사용할 수 없으므로 API 서버를 추가하고, GitHub Pages 단독 배포 계획을 서버와 Redis를 지원하는 배포 방식으로 변경했다.
- 다음 작업: C# 솔루션을 구성하고 계산 엔진의 첫 실패 테스트를 작성한다.

### 2026-09-14 / C# 계산 도메인과 자동 테스트 구현

- 목적: UI와 저장소에 의존하지 않는 계산·검증 규칙을 완성한다.
- 변경 내용 및 파일: `ArtifactGrade.Domain`에 12개 부옵션의 최고 증가량, 6개 평가 프로필, C~SSS 등급 구간, 부위·주옵션 데이터와 입력 검증을 구현했다. `ArtifactGrade.Domain.Tests`에 xUnit 테스트 10개를 작성했다.
- 결정과 이유: `스탯 수치 ÷ 최고 1회 증가량 × 가중치 × 10`으로 계산하여 사용자가 결과를 손으로 재현할 수 있게 했다. 주옵션은 부옵션 점수에 섞지 않고 존재 가능한 조합과 동일 부옵션 여부만 검증한다.
- 검증 방법 및 결과: 테스트를 먼저 실패시킨 뒤 최소 구현을 추가하는 방식으로 진행했다. 최고 증가량 계산, 오류 3종, 격파 프로필, 전체 프로필, 표시 데이터, 부위·주옵션, 주옵션·부옵션 중복, 강화 단계별 최대치, 정의되지 않은 enum과 null 목록 테스트가 모두 통과했다.
- 문제와 해결: 처음에는 외부 테스트 패키지 복원이 지연되어 패키지 없는 C# 검증 실행기를 사용했다. NuGet 패키지가 확보된 뒤 동일한 7개 동작을 표준 xUnit 테스트로 옮겨 다시 통과시켰다.
- 다음 작업: API와 Redis 저장, Blazor UI를 연결한다.

### 2026-09-14 / ASP.NET Core API와 Redis 저장 구현

- 목적: 계산 기능을 HTTP로 제공하고 사용 기록을 Redis에 저장한다.
- 변경 내용 및 파일: `POST /api/scores`, `GET /api/health`, `RedisCalculationStore`, CORS와 문자열 enum JSON 설정을 추가했다. Redis List에 최근 20개 계산을 저장하고 별도 키에 누적 횟수를 기록한다.
- 결정과 이유: Redis가 꺼져도 계산 자체는 사용할 수 있도록 저장 오류를 잡아 `redisSaved=false`와 경고 문구를 반환한다. 오류는 서버 경고 로그에도 남긴다.
- 검증 방법 및 결과: `tests/api-smoke.ps1`로 치명타 프로필 샘플이 50점 A등급을 반환함을 확인했다. 로컬 Redis가 없는 상태에서 저장 실패 경고와 `redisSaved=false`도 확인했다.
- 문제와 해결: Windows Event Log 기록 권한이 없어 첫 HTTP 응답이 중단되었다. API 로깅 공급자를 Console로 제한해 운영 로직과 무관한 권한 의존성을 제거했다.
- 다음 작업: Redis가 실행되는 Docker 환경에서 실제 저장과 최근 20개 제한을 통합 검증한다.

### 2026-09-14 / Blazor UI 구현과 브라우저 검증

- 목적: 계산기를 설치 없이 사용할 수 있는 반응형 웹 화면으로 제공한다.
- 변경 내용 및 파일: 프로필, 부위, 주옵션, 강화 단계, 부옵션 4개 입력 화면과 점수·등급·기여도 결과 화면을 구현했다. 입력 복원, 초기화, 결과 복사, Redis 저장 상태 표시를 추가했다.
- 결정과 이유: UI는 입력과 결과 표시만 담당하고 모든 유효성 판단과 점수 계산은 서버와 Domain이 맡는다. 표시 이름도 `RelicCatalog`에서 관리하여 UI에 같은 데이터를 중복 저장하지 않았다.
- 검증 방법 및 결과: 데스크톱 브라우저에서 치명타 확률 6.48, 치명타 피해 12.96, 속도 5.2, 공격력 8.64를 입력해 66점 SS등급과 20·20·16·10점의 기여도를 확인했다. 결과 복사와 새로고침 후 입력 복원을 확인했다. 390px 모바일 폭에서 문서 너비와 화면 너비가 같아 가로 넘침이 없음을 확인했다.
- 문제와 해결: 숫자 입력이 포커스를 잃기 전 C# 상태에 반영되지 않아 빈 입력으로 계산되는 문제를 `oninput` 바인딩으로 해결했다. API와 클라이언트의 enum JSON 설정 불일치도 문자열 enum 설정을 공유하는 방식으로 해결했다.
- 다음 작업: 실제 유물 10개를 입력해 프로필 가중치와 사용성을 평가한다.

### 2026-09-14 / 실행·배포·사용 문서 정리

- 목적: 새로운 사용자가 문서만 보고 실행하고 검증할 수 있게 한다.
- 변경 내용 및 파일: `README.md`, `.gitignore`, `NuGet.Config`, Dockerfile 2개, `docker-compose.yml`, Nginx 설정, GitHub Actions, MIT 라이선스를 추가했다.
- 결정과 이유: Docker Compose에서 Client, API, Redis를 한 번에 실행하도록 구성했다. GitHub Actions는 패키지 복원, xUnit 테스트, Release 빌드를 수행한다.
- 검증 방법 및 결과: Client와 API의 Debug 빌드는 각각 경고 0개, 오류 0개로 통과했다. Docker 실행 파일이 현재 환경에 없어 컨테이너 전체 실행은 아직 검증하지 못했다.
- 문제와 해결: GitHub Pages만으로는 API와 Redis를 실행할 수 없으므로 Docker 지원 호스팅이 필요함을 README에 명시했다.
- 다음 작업: 전체 Release 빌드와 코드 리뷰 후 GitHub에 업로드한다.

### 2026-09-14 / 최종 로컬 품질 검증

- 목적: 최신 UI·도메인·API 변경을 하나의 Release 결과물로 검증하고 공개 전 오류 처리를 다듬는다.
- 변경 내용 및 파일: 강화 단계별 부옵션 최대치 검증과 회귀 테스트를 추가했다. `/api/health`가 내부 Redis 예외 문구를 응답에 노출하지 않고 서버 로그에만 남기도록 변경했다. 실제 API 계약에 맞게 `ArtifactGrade.Api.http` 예제를 교체하고 개발 블로그 게시 초안을 작성했다.
- 결정과 이유: 5성 유물은 3레벨마다 부옵션이 추가 또는 강화되므로 한 부옵션의 최대 증가 횟수를 `1 + 강화 단계 / 3`으로 계산한다. 상태 API는 이용자에게 해결 가능한 안내만 제공하고 상세 예외는 로그가 소유한다.
- 검증 방법 및 결과: `dotnet build ArtifactGrade.slnx -c Release --no-restore --disable-build-servers`가 경고 0개, 오류 0개로 통과했다. xUnit 10개가 모두 통과했다. API 프로세스를 실행한 뒤 스모크 테스트에서 50점 A등급, `redisSaved=false`, 잘못된 enum 입력의 `400 Bad Request`를 확인했다.
- 문제와 해결: 빌드와 테스트를 동시에 실행해 C# 중간 DLL 잠금이 발생했다. 두 검증을 순차 실행해 해결했다. Windows 실행 정책이 `.ps1` 직접 실행을 차단해 별도 PowerShell 프로세스에 `-ExecutionPolicy Bypass`를 지정했다.
- 다음 작업: 검증된 변경을 Git 브랜치에 커밋·푸시한다. Docker가 있는 환경에서는 Redis 연결 성공 경로를 추가 검증한다.

### 2026-09-14 / 독립 코드 리뷰와 수정

- 목적: 저장소 코딩 규칙과 사용자 요구사항을 별도 관점으로 검토하여 공개 전 결함을 제거한다.
- 변경 내용 및 파일: 두 개의 읽기 전용 리뷰에서 보고된 잘못된 enum·null API 입력, 계산 후 프로필 변경 시 결과 라벨 불일치, 손상된 브라우저 저장값, Redis 연결 동시성·종료 처리, Redis 외부 포트 노출, 운영 API 주소 문제와 CI 설명 불일치를 수정했다. `redis-integration.ps1`을 추가해 저장 성공, 최근 20개 제한, 누적 횟수를 Docker 환경에서 확인할 수 있게 했다.
- 결정과 이유: 계산 결과는 제출 당시 프로필과 함께 저장해 이후 입력 변경과 분리했다. 운영 클라이언트는 Nginx의 같은 출처 `/api` 프록시를 사용하고, 로컬 개발에서만 별도 API 주소를 설정한다. Redis 호스트 포트는 `127.0.0.1`에만 바인딩한다.
- 검증 방법 및 결과: 수정 후 전체 Release 빌드가 경고 0개, 오류 0개로 통과했고 xUnit 10개와 API 스모크 테스트가 통과했다. 브라우저에서 66점 SS 결과를 만든 뒤 입력 프로필을 변경해도 결과가 치명타 프로필로 유지되는 것을 확인했다. 새 탭에서도 입력, 점수, 등급, 기여도가 함께 복원됐다. 두 리뷰 관점의 재검토에서 기존 지적 해결을 확인했고 남은 P0~P2 문제는 없었다.
- 문제와 해결: 브라우저 재접속 시 Blazor WebAssembly 로딩이 끝나기 전에 확인하면 연결 실패 또는 로딩 화면만 보였다. 서버 수신 상태와 애플리케이션 로딩 완료 후 DOM을 다시 확인했다.
- 다음 작업: Docker가 설치된 환경에서 `redis-integration.ps1`을 실행하고 외부 배포 주소를 확정한다.

### 2026-09-14 / GitHub 브랜치 업로드

- 목적: 결과물을 다른 사람이 확인하고 실행할 수 있도록 GitHub에 정리해 업로드한다.
- 변경 내용 및 파일: `codex/csharp-relic-scorer` 브랜치를 생성하고 구현, 테스트, 실행·배포 설정, README, 개발 기록, 블로그 초안을 커밋했다.
- 결정과 이유: 기본 브랜치를 바로 변경하지 않고 검토 가능한 기능 브랜치로 올려 기존 저장소 이력을 보존했다.
- 검증 방법 및 결과: 커밋 `280c6c8` 생성과 원격 추적 브랜치 설정을 확인했다. 업로드 주소는 `https://github.com/arke0909/Honkai-Starrail-Artifact-Grade/tree/codex/csharp-relic-scorer`다.
- 문제와 해결: 샌드박스가 `.git` 참조 생성을 제한하여 승인된 Git 작업으로 브랜치 생성, 스테이징, 커밋, 푸시를 수행했다.
- 다음 작업: Pull Request를 검토·병합하고 외부 배포와 게시를 진행한다.

## 13. 트러블슈팅

### NuGet.Config 접근 거부 및 복원 지연

- 증상: `dotnet restore`가 사용자 프로필의 `NuGet.Config`에 접근하지 못하거나 NuGet.org 응답을 기다리다 타임아웃됐다.
- 원인: 샌드박스에서 사용자 프로필 설정 파일 읽기가 제한되었고 첫 패키지 다운로드가 지연됐다.
- 해결: 저장소 루트에 필요한 소스만 선언한 `NuGet.Config`를 추가하고 `--configfile NuGet.Config`로 복원했다. 확보된 로컬 패키지는 `--ignore-failed-sources`로 사용할 수 있다.
- 재발 방지: README와 Dockerfile, CI가 모두 저장소의 `NuGet.Config`를 명시적으로 사용한다.

### API가 빈 응답으로 연결을 종료함

- 증상: 서버가 실행 중인데 `/api/scores` 요청은 빈 응답으로 끝났다.
- 원인: Windows Event Log 로깅 공급자가 권한 없는 이벤트 로그에 HTTPS 경고를 기록하려다 예외를 발생시켰다.
- 해결: API 로깅 공급자를 Console로 제한하고, HTTPS 종료는 운영 리버스 프록시가 담당하도록 애플리케이션의 강제 리디렉션을 제거했다.
- 재발 방지: API 스모크 테스트로 실제 HTTP 응답을 확인한다.

### 화면에 입력했지만 부옵션이 없다고 표시됨

- 증상: 숫자 4개가 화면에 보이는데도 부옵션 개수 오류가 반환됐다.
- 원인: 기본 바인딩이 `change` 이벤트에서 동작해 계산 버튼을 누른 시점에 마지막 입력이 C# 상태에 반영되지 않았다.
- 해결: 숫자 입력을 `oninput` 이벤트에 바인딩했다.
- 재발 방지: 브라우저에서 실제 입력·클릭 흐름을 검증한다.

### API 응답 뒤 연결 실패 문구가 표시됨

- 증상: API는 정상 응답했지만 클라이언트가 연결 실패로 표시했다.
- 원인: API는 enum을 문자열로 보냈고 클라이언트는 기본 숫자 enum 형식으로 읽었다.
- 해결: 양쪽에 `JsonStringEnumConverter`를 적용하고 연결 오류와 JSON 오류 메시지를 분리했다.
- 재발 방지: API 스모크 테스트와 브라우저 계산 테스트를 함께 실행한다.

### Redis가 없는 개발 환경

- 증상: 점수는 계산되지만 Redis 저장 경고가 표시된다.
- 원인: 현재 검증 환경에 Docker와 `redis-server`가 설치되어 있지 않다.
- 해결: 저장 실패를 숨기지 않고 결과에 경고를 포함했다. Docker가 있는 환경에서는 `docker compose up --build`로 Redis까지 실행한다.
- 재발 방지: 외부 배포 전 `/api/health`가 `healthy`, `redis=connected`를 반환하는지 확인한다.

### Release 빌드와 테스트 동시 실행 시 DLL 잠금

- 증상: `ArtifactGrade.Domain.dll`을 `VBCSCompiler`가 사용 중이라는 `CS2012` 오류가 발생했다.
- 원인: 같은 프로젝트를 참조하는 전체 빌드와 테스트 빌드를 동시에 실행해 동일한 `obj/Release` 파일에 쓰기를 시도했다.
- 해결: 전체 Release 빌드를 먼저 끝낸 뒤 `dotnet test --no-build`를 순차 실행했다.
- 재발 방지: 동일 구성의 빌드와 테스트는 병렬 실행하지 않고 CI와 로컬에서 순서를 고정한다.

### Windows에서 API 스모크 테스트 스크립트 실행 차단

- 증상: API는 정상 기동했지만 `api-smoke.ps1`이 `PSSecurityException`으로 실행되지 않았다.
- 원인: 현재 Windows 사용자의 PowerShell 실행 정책이 로컬 스크립트 실행을 허용하지 않았다.
- 해결: `powershell -NoProfile -ExecutionPolicy Bypass -File tests/api-smoke.ps1`로 해당 프로세스에만 우회 정책을 적용했다.
- 재발 방지: README의 스모크 테스트 명령에 프로세스 단위 실행 정책 옵션을 포함한다.

## 14. 실사용 후기

### 2026-09-14 / 개발 중 샘플 사용 후기

- 사용 환경: 데스크톱 브라우저와 390px 모바일 화면
- 사용 데이터: 치명타 딜러용 샘플 유물 1개
- 장점: 입력과 결과를 한 화면에서 비교할 수 있고, 스탯별 기여도를 보여줘 점수의 근거가 분명하다. 새로고침 후 입력 복원과 결과 복사가 편리하다. 모바일에서 가로 스크롤이 생기지 않았다.
- 단점: 범용 프로필이므로 특정 캐릭터의 속도·효과 명중 목표치까지 반영하지 못한다. 주옵션 적합도는 점수에 포함되지 않는다. Redis가 없는 환경에서는 저장 실패 확인을 기다리는 시간이 발생한다.
- 발견한 오류: 입력 반영 시점과 enum JSON 형식 불일치를 실제 조작 과정에서 발견해 수정했다.
- 개선 우선순위: Redis 연결 환경 통합 검증, 실제 유물 10개 평가, 캐릭터별 프로필, 주옵션 적합도 순서로 개선한다.

이 기록은 개발 검증 후기이므로 평가 항목 4의 완료 증거로 사용하지 않는다. 실제 보유 유물 10개 이상을 평가한 후 별도의 최종 후기를 작성한다.

### 2026-09-15 / 실제 유물 48개 최종 사용 후기

- 사용 환경과 데이터: 배포 이미지와 같은 통합 실행 결과를 데스크톱 및 390px 모바일 브라우저에서 열고, 공개 UID `800333171`의 캐릭터 8명·5성 장착 유물 48개를 불러왔다.
- 실제 결과: 에버나이트 유물 6개는 평균 82.1점 S, S 5개·A 1개였고 핵심 주옵션은 6/6이었다. 연결 매듭이 73.2점으로 가장 낮아 교체 우선으로 표시됐다. 카스토리스와 아케론 탭에서는 HP 기반과 공격력 기반 가중치가 각각 다르게 바뀌는 것을 확인했다.
- 장점: UID 한 번으로 수동 입력 없이 유물을 가져오며, 평균과 교체 우선을 먼저 보고 각 유물의 주옵션 계수·12개 부옵션 유효도·기여 점수로 이유를 추적할 수 있다. 캐릭터 탭을 바꿀 때 같은 유물 수치가 캐릭터 기준에 맞춰 즉시 재평가된다.
- 단점: UID 방식은 공개 프로필에 전시한 장착 유물만 가져온다. 파티, 광추, 성혼, 속도·효과 명중 목표 구간과 치명타 초과 같은 비선형 조건은 계산하지 않는다. 외부 프로필·이미지 서비스 상태에 따라 첫 조회가 늦어질 수 있다.
- 개선점: 속도·효과 명중 목표 구간과 파티 조건을 선택형 보정으로 제공하고, Redis 최근 계산 기록을 조회하는 화면을 후속 기능으로 검토한다.
- 화면 검증: 390px에서 문서 너비와 스크롤 너비가 모두 375px이고 유물 카드 6개의 내부 가로 넘침은 0건이었다. 데스크톱과 모바일 모두 브라우저 오류·경고가 0건이었다.

## 15. 게시 및 배포 정보

- GitHub 저장소: https://github.com/arke0909/Honkai-Starrail-Artifact-Grade/tree/codex/csharp-relic-scorer
- 배포 사이트: 클라이언트·API·Redis 배포 후 링크 기록
- 개발 블로그 또는 SNS: 게시 후 링크 기록
- 최종 점검일: 2026-09-14 (로컬 기능 검증 기준)

## 16. 유물 정보 자동 가져오기 조사

### 2026-09-14 / 수동 입력을 줄이는 가져오기 방식 비교

- 목적: 유물 주옵션과 부옵션을 하나씩 입력하지 않고 계산기에 전달할 수 있는 경로를 조사한다.
- 조사 내용: 공개 UID 조회, HSR Scanner JSON, Reliquary Archiver JSON/WebSocket, HoYoLAB 응답 가져오기, 스크린샷 OCR을 비교했다.
- 결정과 이유: 첫 구현은 설치가 필요 없는 UID 공개 프로필 조회와 Redis 캐시로 하고, 전체 인벤토리는 스캐너 JSON 업로드로 보완한다. 로그인 쿠키 수집과 애플리케이션 내 패킷 캡처는 보안·운영 범위 때문에 기본 경로에서 제외한다.
- 검증 방법과 결과: 각 도구와 API의 공식 프로젝트 문서에서 데이터 범위, 실행 조건, 출력 형식과 알려진 제약을 대조했다. 공개 UID 방식은 전시 캐릭터 유물만 가능하고 전체 가방은 스캐너 내보내기가 필요하다는 결론을 얻었다.
- 상세 문서: `docs/research/AUTOMATIC_RELIC_IMPORT.md`
- 다음 작업: 사용자 승인 후 UID 가져오기 API부터 구현하고, 외부 응답 샘플을 계약 테스트로 고정한다.

### 2026-09-14 / UID 및 Scanner JSON 자동 가져오기 구현

- 목적: 유물의 강화 단계, 부위, 주옵션과 부옵션을 하나씩 입력하지 않아도 공개 프로필 또는 스캐너 파일에서 계산할 수 있게 한다.
- 변경 내용 및 파일: `RelicImport.cs`에 공통 가져오기·일괄 점수 계약을 추가하고, API에 `MihomoRelicParser`, `MihomoRelicImporter`, `HsrScannerRelicParser`를 구현했다. `GET /api/import/uid/{uid}`, `POST /api/import/scanner`, `POST /api/scores/batch`를 추가했다. Blazor 화면에는 UID 입력, JSON 파일 선택, 유물 적용, 전체 계산과 점수 정렬 UI를 추가했다.
- Redis 활용: MiHoMo 원본 응답을 `artifact-grade:imports:uid:{UID-SHA256}` 키에 5분 동안 캐시한다. UID 원문은 키에 저장하지 않는다. Redis가 없는 환경에서도 캐시만 생략하고 UID 조회를 계속한다.
- 결정과 이유: UID 경로는 설치가 없지만 공개 장착 유물만 제공한다. 전체 인벤토리는 HSR Scanner 버전 4 호환 JSON으로 보완한다. HoYoLAB 로그인 쿠키 수집과 애플리케이션 내 패킷 캡처는 구현하지 않았다.
- 입력 보호: UID 숫자 9자리 검증과 호출자 IP별 분당 10회 호출 제한, JSON 최대 10MB, Scanner 버전 4 확인, 5성 필터, 지원하지 않는 스탯 경고, 일괄 요청 최대 300개와 null 항목 거부를 적용했다. 잘못된 UID는 호출 제한 횟수에 포함하지 않는다. 클라이언트는 300개씩 나누어 전체 목록을 계산한다.
- TDD 결과: MiHoMo 필드 변환, Redis 캐시 재사용, 지원하지 않는 유물 경고, Scanner 캐릭터·부위·주옵션·부옵션 변환, 잘못된 문서 오류 정규화, 일괄 점수 계산, 5성이 아닌 유물 거부와 외부 소수 오차 허용 테스트를 실패부터 확인한 뒤 구현했다.
- 실제 API 검증: Docker와 Redis 없이 MiHoMo 예제 UID `100000999`에서 5성 장착 유물 42개를 가져왔다. Scanner 샘플은 Bronya 장착, Hands, FlatAttack으로 변환됐고 일괄 계산은 40점 B등급을 반환했다.
- 브라우저 검증: UID 42개 표시, 선택 유물 입력 적용, 42개 전체 계산과 내림차순 정렬을 확인했다. 390px 화면의 문서 너비와 화면 너비가 같아 가로 넘침이 없었고 브라우저 오류 로그는 0개였다.
- 문제와 해결 1: 실행 중인 API가 변경 전 빌드여서 `/api/scores/batch`가 404를 반환했다. API를 다시 빌드·재시작한 뒤 정상 응답을 확인했다.
- 문제와 해결 2: `127.0.0.1`로 연 클라이언트는 개발 CORS 허용 주소인 `localhost`와 출처가 달라 API 호출이 차단됐다. README에 적힌 `http://localhost:5111`로 통일해 검증했다.
- 문제와 해결 3: MiHoMo의 정확한 실수 값이 로컬 최고 증가량 상수를 약 `0.00004` 초과하는 경우가 있었다. 실제로 가능한 값을 거부하지 않도록 최대치 검증에 `0.001` 허용 오차를 추가하고 큰 초과값을 거부하는 기존 테스트는 유지했다.
- 문제와 해결 4: Windows PowerShell 5.1이 BOM 없는 UTF-8 테스트 스크립트의 한글 문자열을 잘못 해석했다. 스모크 테스트 메시지를 ASCII로 바꿔 실행 환경에 따른 파싱 오류를 제거했다.
- 리뷰와 수정: 명세·코드 품질 두 관점으로 변경분을 검토했다. 필수 JSON 속성 누락 시 500이 되던 문제, UID 외부 호출 제한 부재, 일괄 계산 중 프로필·가져오기 상태가 바뀌는 문제, 일괄 목록의 null 항목 문제를 수정했다. 재검토에서 일괄 검증 오류 상세 누락, 전체 사용자가 공유하던 호출 제한, Docker 없는 실행 안내의 모호함을 추가로 찾아 수정했다. 일괄 계산은 시작 시 프로필과 목록을 스냅샷으로 고정한다.
- 프록시 처리: Docker 배포에서는 Nginx 주소만 호출자로 인식될 수 있어 신뢰된 프록시 전달 헤더 처리를 호출 제한보다 앞에 추가했다. Compose는 전용 `172.30.0.0/24` 네트워크만 신뢰하고 API 호스트 포트를 `127.0.0.1`에 바인딩해 임의 외부 요청이 전달 헤더를 위조하지 못하게 했다. 로컬 신뢰 프록시 테스트에서 첫 IP는 10회 200 후 429가 됐지만 두 번째 IP의 첫 요청은 200으로 유지되어 제한이 분리됨을 확인했다.
- 최종 검증: 전체 xUnit 테스트 19개가 통과했고 Release 빌드는 경고 0개, 오류 0개였다. Docker 없이 Scanner 가져오기·40점 일괄 계산을 다시 통과했다. UID 호출 제한은 로컬 MiHoMo 대역 서버에서 검증했다. 잘못된 UID 11회는 모두 400이었고, 이어서 유효한 UID를 11회 보내 같은 호출자 IP의 앞 10회는 200, 마지막 요청은 JSON 안내와 함께 429가 되는 것을 확인했다.
- 사용·테스트 문서: `README.md`, `ArtifactGrade.Api.http`, `tests/import-smoke.ps1`, `docs/research/AUTOMATIC_RELIC_IMPORT.md`를 갱신했다.
- 다음 작업: 실제 사용자 UID와 Scanner 전체 파일로 사용 후기를 작성하고 외부 배포 환경에서 Redis 캐시 적중을 확인한다.

### 2026-09-14 / 캐릭터별 유물 보기 구현

- 목적: 가져온 유물을 하나의 긴 목록에서 찾지 않고 장착 캐릭터별로 구분해 확인한다.
- 변경 내용 및 파일: `RelicImport.cs`에 캐릭터별 그룹 규칙과 `미장착` 그룹을 추가했다. `Home.razor`에는 캐릭터 이름과 유물 개수를 표시하는 탭을 추가하고, 선택한 캐릭터의 유물 선택 목록과 일괄 점수 결과만 표시하도록 연결했다. `Home.razor.css`에는 가로 스크롤이 가능한 반응형 탭 스타일을 추가했다.
- 동작 기준: 장착 캐릭터가 있는 그룹은 가져온 순서를 유지하며 먼저 표시하고, 캐릭터 정보가 없거나 빈 유물은 마지막 `미장착` 탭에 모은다. `전체 점수 계산`은 모든 유물을 한 번 계산하지만 결과 화면은 현재 캐릭터 탭에 해당하는 항목만 보여준다.
- TDD 결과: Bronya 유물 2개, Seele 유물 1개, 미장착 유물 1개를 입력해 `Bronya → Seele → 미장착` 순서와 각 그룹 내용이 정확한지 실패 테스트부터 작성했다. 구현 후 전체 테스트 20개가 통과했다.
- 브라우저 검증: 로컬 MiHoMo 대역 데이터에서 Bronya 2개와 Seele 1개 탭이 생성됐다. 탭 전환 시 유물 선택 목록이 각각 2개와 1개로 바뀌었고, 전체 계산 후 점수 결과도 각각 2행과 1행만 표시됐다.
- 문제와 해결: 첫 UI 빌드에서 Razor가 `@group.Relics.Count개`를 하나의 속성명으로 해석해 컴파일 오류가 발생했다. 값을 `@(group.Relics.Count)개`로 명확히 분리했다. 브라우저 점검에서는 선택 상태의 접근성 표기를 확인했고, 표준 키보드 탭 동작이 없는 화면에 `tab` 역할을 쓰지 않도록 일반 버튼 그룹과 `aria-pressed`로 수정했다. 최종 코드 검토에서는 `미장착` 표시 문구가 Domain과 Client에 중복된 점을 찾아 화면 표시 책임이 있는 Client 한곳으로 모았다.
- 사용법: UID 또는 Scanner JSON으로 유물을 불러온 뒤 `캐릭터별 보기` 탭에서 캐릭터를 선택한다. 탭 아래 유물 선택과 점수 결과는 선택한 캐릭터 기준으로 바뀌며, Scanner의 미장착 유물은 `미장착` 탭에서 확인한다.

### 2026-09-14 / 개발 서버 원클릭 실행 추가

- 목적: 코드를 수정할 때마다 API와 클라이언트 PowerShell을 직접 종료하고 다시 실행하는 작업을 없앤다.
- 변경 내용 및 파일: 저장소 루트에 `start-dev.cmd`를 추가하고 `README.md`의 Docker 없는 실행 방법을 원클릭 실행 기준으로 갱신했다.
- 동작: 실행 파일이 .NET SDK를 확인한 뒤 API와 클라이언트를 별도 창에서 `dotnet watch`로 실행한다. 이미 5072·5111 포트의 서비스가 응답하면 중복 실행하지 않는다. 클라이언트가 준비될 때까지 최대 60초 기다린 후 브라우저를 자동으로 연다.
- 자동 반영: 실행 후 C#·Razor·CSS 파일을 저장하면 `dotnet watch`가 변경을 감지해 다시 빌드하거나 Hot Reload를 적용한다. 강제 재시작은 실행 창에서 `Ctrl+R`, 종료는 `Ctrl+C`를 사용한다.
- 오류 안내: .NET SDK가 없으면 설치 안내를 표시하고, 클라이언트가 60초 안에 준비되지 않으면 API·클라이언트 창의 오류를 확인하도록 안내한다. Redis와 Docker가 없어도 실행되며 저장·캐시 기능만 경고와 함께 생략된다.
- 실제 실행 점검: 첫 실행에서 API·클라이언트 감시 창과 웹 화면이 자동으로 열렸고 클라이언트가 HTTP 200으로 응답했다. Redis가 없는 API 상태 주소는 정상적으로도 HTTP 503을 반환하므로, 상태 코드가 아니라 서버 응답 성공 여부로 기존 API 프로세스를 판별하도록 중복 실행 검사를 수정했다.
- 최종 검증: 수정 후 실행 파일을 다시 실행했을 때 API와 클라이언트를 모두 기존 프로세스로 인식하고 중복 실행 없이 브라우저만 열었다. 전체 xUnit 테스트 20개가 통과했고 Release 빌드는 경고 0개, 오류 0개였다.
- 사용법: 최초 한 번 `dotnet restore ArtifactGrade.slnx --configfile NuGet.Config`를 실행한 뒤 파일 탐색기에서 `start-dev.cmd`를 더블클릭한다.

### 2026-09-14 / 가져온 유물 주옵션 표시 추가

- 목적: 일괄 점수 목록에서 부옵션뿐 아니라 각 유물의 주옵션도 바로 구분한다.
- 변경 내용 및 파일: `Home.razor`의 유물 옵션 요약을 `RelicStatSummary`로 정리해 주옵션 이름과 단위를 먼저 표시하고 기존 부옵션 수치를 이어서 표시했다. 데이터 계약과 점수 계산 방식은 변경하지 않았다. `README.md`와 `BLOG_POST_DRAFT.md`에도 표시 방식과 사용법을 추가했다.
- 표시 형식: `주옵션 {이름}{단위} · 부옵션 {이름 수치 단위} · …` 형식을 사용한다. 예를 들어 공격력 백분율 몸통 유물은 `주옵션 공격력% · 부옵션 치명타 확률 6.48% · 치명타 피해 12.96% · 속도 5.2 · 공격력 21.17`로 보인다.
- 검증 방법: 먼저 Client Release 빌드로 Razor와 C# 컴파일을 확인했다. 이후 MiHoMo 계약과 같은 로컬 응답에서 Jingliu의 5성 몸통 유물을 불러오고 `전체 점수 계산`을 실행해 주옵션이 부옵션 앞에 표시되는지 브라우저 DOM에서 확인했다. 브라우저 오류·경고 로그는 0개였다.
- 검증 중 문제와 해결: 검증 환경에서 외부 MiHoMo 연결이 제한되어 공개 UID 요청이 실패했다. 테스트에서 사용 중인 동일 계약의 로컬 MiHoMo 대역 응답으로 전환해 화면부터 API 변환까지 같은 사용자 흐름을 검증했다. 짧은 검증용 서버 실행 시간이 끝나 연결이 끊긴 경우에는 제한 시간을 늘린 독립 프로세스로 다시 실행했다.
- 리뷰와 수정: 처음에는 선택한 부위의 주옵션 목록에서 이름을 찾았다. 형식이 잘못된 Scanner 파일이 부위와 맞지 않는 주옵션 조합을 만들면 점수 검증 결과를 표시하기 전에 화면 렌더링이 실패할 수 있어, 알려진 전체 주옵션 사전에서 표시 이름을 찾도록 분리했다. 부위·주옵션 조합 오류는 기존 점수 검증이 `계산 불가`로 안내한다.
- 최종 검증: 전체 xUnit 테스트 20개가 통과했고 Release 빌드는 경고 0개, 오류 0개였다. 코드 포맷 검사도 변경 없이 통과했다.
- 재빌드 확인: 2026-09-14 사용자 요청으로 `dotnet build ArtifactGrade.slnx -c Release --no-restore --nologo`를 다시 실행했다. Domain, API, Client와 두 테스트 프로젝트가 모두 빌드됐고 경고 0개, 오류 0개였다.
- 실행 확인: 2026-09-14 사용자 요청으로 `start-dev.cmd`를 실행해 API와 클라이언트 감시 프로세스를 기동하고 브라우저를 열었다. 클라이언트는 HTTP 200으로 정상 응답했으며, API 상태 주소의 HTTP 503은 Redis가 설치되지 않은 현재 환경에서 저장·캐시 연결이 빠졌음을 알리는 예상 결과다.
- 사용법: UID 또는 Scanner JSON으로 유물을 불러오고 `전체 점수 계산`을 누른다. 캐릭터별 점수 목록에서 유물 이름 아래의 `주옵션`과 `부옵션` 요약을 확인한다.

### 2026-09-14 / 캐릭터 스킨·유물 이미지와 주옵션 계수 표시

- 목적: 캐릭터별 탭을 선택했을 때 캐릭터 전신 이미지와 장착 유물 전체를 한 화면에서 비교하고, 주옵션의 종류뿐 아니라 실제 수치까지 확인한다.
- 사전 확인: 기존 전체 테스트 20개가 통과하는 깨끗한 기준 상태를 확인했다. MiHoMo v2 공개 응답에서 캐릭터 `portrait`, 유물 `icon`, 주옵션 `main_affix.value` 필드를 확인했고, StarRailRes의 `relic_main_affixes.json`에서 5성 주옵션 기본값과 강화 단계별 증가량을 대조했다.
- 데이터 변경: `RelicImport.cs`에 `ImportedCharacter`, 유물 `MainStatValue`와 `ImageUrl`을 추가했다. `MihomoRelicParser`는 상대 이미지 경로를 StarRailRes 원본 URL로 바꾸고 퍼센트 주옵션 수치를 화면 단위로 변환한다. `HsrScannerRelicParser`는 캐릭터 `id`와 유물 `set_id`·부위로 이미지 주소를 구성하고 `RelicMainStatValues`로 5성 주옵션 계수를 계산한다.
- 화면 변경: `Home.razor`에서 개별 유물 선택 목록과 별도 점수 목록을 없애고, 선택 캐릭터의 공개 프로필 전신 이미지를 왼쪽에 크게 배치했다. 오른쪽에는 머리·손·몸통·다리·차원 구체·연결 매듭 카드가 최대 6개 모두 보이며, 각 카드에 유물 이미지, 세트, 강화 단계, 주옵션 계수, 부옵션, 점수, `입력 적용` 버튼을 넣었다. `Home.razor.css`는 데스크톱 3열, 중간 화면 2열, 좁은 모바일 1열로 조정했다.
- TDD 과정: 이미지와 주옵션 수치가 아직 없는 상태에서 API 테스트를 실행해 예상한 컴파일 실패를 확인했다. 이후 MiHoMo 이미지·43.2% 변환, Scanner +15 손 유물의 공격력 352.8 계산, 주요 주옵션 7종의 강화 수치 테스트를 구현했다.
- 실제 브라우저 검증: 공개 예제 UID `800333171`에서 캐릭터 8명과 5성 유물 48개를 불러왔다. 선택 캐릭터의 초상화 1개와 유물 이미지 6개가 모두 로드됐고, 캐릭터가 유물보다 왼쪽에 배치됐다. 머리 HP 705.6, 손 공격력 352.8, 몸통 치명타 피해 64.8%, 연결 매듭 에너지 회복효율 19.44%를 확인했다.
- 상호작용 검증: `전체 점수 계산` 후 선택 캐릭터의 6개 카드에 등급과 점수가 표시됐다. 다른 캐릭터 탭으로 전환했을 때 캐릭터 이미지와 유물 6개가 함께 교체됐으며, 브라우저 오류·경고 로그는 0개였다.
- 실행 중 문제와 해결: 첫 브라우저 확인에서 기존 `dotnet watch` 프로세스가 이전 화면을 계속 제공했다. 이 저장소의 API·클라이언트 프로세스만 식별해 종료한 뒤 `start-dev.cmd`로 재시작하여 새 계약과 화면을 반영했다. PowerShell 실행 정책으로 스모크 스크립트가 차단된 경우 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests\\import-smoke.ps1`로 프로젝트 스크립트만 실행했다.
- 검증 결과: 전체 xUnit 테스트 28개가 통과했고 Release 빌드 과정에서 경고와 오류가 없었다. 실행 중인 API를 대상으로 Scanner 가져오기와 40점 일괄 계산 스모크 테스트도 통과했다.
- 리뷰와 수정: 선택적 이미지 필드의 형식 오류가 전체 UID 조회나 정상 유물을 실패시키지 않도록 문자열이 아닌 이미지 경로는 `null`로 격리하고 화면의 `이미지 정보 없음` 표시로 드러냈다. 캐릭터와 유물의 연결은 표시 이름이 아니라 `CharacterId`를 원본으로 사용하도록 바꿨으며, 주옵션 수치가 없는 상태는 모델 생성 시 명시적으로 전달하고 화면에는 `수치 정보 없음`으로 표시한다.
- 사용법: `start-dev.cmd`를 더블클릭하고 `http://localhost:5111`에서 UID를 입력한 뒤 `UID 불러오기`를 누른다. 캐릭터 탭을 선택하면 왼쪽의 공개 프로필 전신 이미지와 오른쪽의 장착 유물 전체가 표시된다. 프로필을 선택하고 `전체 점수 계산`을 누르면 각 유물 카드에서 점수를 확인할 수 있으며, `입력 적용`은 해당 유물을 아래 직접 입력 화면으로 복사한다.
- 당시 제한사항: UID 방식은 공개 프로필 캐릭터만 제공하며 이미지 표시에는 인터넷 연결이 필요하다. 이 시점에는 MiHoMo parsed 응답만 확인해 `portrait`를 현재 외형 이미지로 표시했으나, 아래 재조사에서 원본 응답의 `dressedSkinId`를 찾아 이 판단과 구현을 바로잡았다. HSR Scanner는 ID로 기본 캐릭터·유물 이미지를 해석하며, ID가 빠진 오래된 호환 파일만 이미지 자리 안내가 나온다.

### 2026-09-14 / 캐릭터 착용 외형 재조사 및 반영

- 목적: 캐릭터의 외형에 따라 실제 일러스트가 달라지는지 다시 확인하고, 착용 중인 외형을 화면에 정확히 반영한다.
- 조사 결과: Enka HSR 원본 응답의 `detailInfo.avatarDetailList[*].dressedSkinId`가 착용 외형 식별자다. 공개 UID `800333171`의 카스토리스(`avatarId=1407`)에서 `dressedSkinId=1140701`을 확인했고, 지원 캐릭터는 `assistAvatarList`에도 들어갈 수 있어 두 배열을 모두 읽도록 했다. MiHoMo v2 parsed의 `enhanced`는 원본 `enhancedId`를 Boolean으로 바꾼 별도 값이며 외형 판단에 사용할 수 없다.
- 원인 정정: 앞선 구현은 유물·캐릭터 정보를 읽는 MiHoMo parsed 응답만 확인했다. 이 응답은 `dressedSkinId`를 제거하고 기본 `portrait`만 제공하므로 “착용 외형 식별자가 없다”는 잘못된 결론을 냈다. 원본 API와 실제 Enka 프로필 페이지를 함께 대조해 이를 정정했다.
- 구현 내용: `MihomoRelicImporter`가 기존 `sr_info_parsed/{uid}`와 함께 `sr_info/{uid}`를 조회한다. `MihomoAppearanceMetadata`가 일반·지원 캐릭터 목록의 숫자 또는 문자열 ID를 검증해 `캐릭터 ID → dressedSkinId`를 parsed JSON에 병합하고, 같은 JSON을 Redis에 5분 캐시한다. `MihomoRelicParser`는 외형 ID가 있으면 Enka 전신 외형 URL을 우선 선택하고 기본 StarRailRes portrait를 폴백 URL로 함께 전달한다.
- 실패 처리: 원본 외형 조회 실패는 유물 가져오기까지 실패시키지 않는다. 서버 로그와 화면 경고에 실패를 드러내고 기본 캐릭터 이미지를 사용한다. 일부 외형은 `{dressedSkinId}.png` 규칙의 파일이 없을 수 있으므로, 브라우저가 외형 이미지 로드 실패를 감지하면 기본 portrait로 한 번 전환하고 그것도 실패하면 이미지 없음 안내를 표시한다.
- TDD: 먼저 외형 ID가 기본 portrait를 덮어쓰는 테스트와 원본 외형 API 503에서도 유물 가져오기가 유지되는 테스트를 추가했다. `ImportedCharacter.FallbackImageUrl`이 아직 없어 예상한 컴파일 실패를 확인한 뒤 구현했고, 리뷰 보완 테스트까지 포함한 관련 MiHoMo 테스트 8개가 통과했다.
- 개발 중 문제와 해결: 테스트 첫 실행은 코드 실패를 보기 전에 실행 중인 API가 `ArtifactGrade.Domain.dll`을 잠가 MSBuild 복사가 10회 실패했다. `netstat -ano`로 5072·5111 포트의 이 저장소 프로세스만 확인하고 종료한 뒤 다시 실행해 의도한 실패 상태와 구현 후 성공 상태를 확인했다.
- 리뷰와 수정: 명세 관점에서는 누락 기능을 찾지 못했다. 코드 기준 첫 검토에서는 원본 응답에서 `avatarDetailList`와 `assistAvatarList`가 동시에 누락돼도 외형 확인 완료로 캐시되는 문제를 찾아 회귀 테스트와 실패 처리를 추가했다. 재검토에서는 외형 CDN 실패 시 기본 이미지로 조용히 바뀌어 사용자가 오해할 수 있는 문제를 찾았다. 캐릭터 캡션에 `외형 이미지 로드 실패 · 기본 이미지 표시 중`을 낮은 강도로 표시하고, 기본 이미지까지 실패한 경우도 별도 문구로 드러내도록 보완했다.
- 실제 브라우저 검증: 공개 UID `800333171`에서 카스토리스 탭의 이미지 주소가 `AvatarDrawCard/AvatarSkin/1140701.png`인지 확인했다. 이미지는 1457×2031로 정상 로드됐고, 오른쪽 유물 카드 6개와 주옵션 계수 6개가 함께 표시됐다. 새 브라우저 세션의 오류·경고 로그는 0개였다.
- 검증 환경 문제와 해결: Release 빌드가 정적 파일 해시를 바꾼 뒤 기존 `dotnet watch`가 이전 파일명을 제공해 Blazor 동적 모듈 로드가 한 번 실패했다. 5072·5111의 기존 프로세스만 종료하고 최신 Release API·클라이언트를 다시 실행해 해결했다. 자동화 터미널은 `start-dev.cmd`가 여는 새 창의 입력 리디렉션을 지원하지 않아 검증 프로세스만 숨김 `dotnet run`으로 시작했으며, 사용자가 파일 탐색기에서 더블클릭하는 일반 실행법은 그대로다.
- 최종 자동 검증: 전체 xUnit 테스트 31개(도메인 21개, API 10개)가 통과했다. Release 빌드는 경고 0개, 오류 0개였고 `dotnet format --verify-no-changes`와 `git diff --check`도 통과했다.
- 최종 재검토: 외형 실패 안내를 추가한 뒤 코드 기준을 다시 확인했으며 남은 지적 사항은 없었다. UI는 이미지 표시와 폴백 상태만 관리하고 외형 판정·URL 구성은 API가 소유하는 책임 분리를 유지했다.
- 사용법: `start-dev.cmd`를 더블클릭하고 UID를 입력해 불러온다. `캐릭터별 보기`에서 외형을 착용한 캐릭터를 선택하면 왼쪽 이미지가 착용 외형으로 표시된다. 별도 설정이나 외형 ID 입력은 필요 없다.
- 조사 문서: 필드, 이미지 경로, 예외와 구현 판단은 `docs/research/CHARACTER_APPEARANCE.md`에 1차 자료 링크와 함께 정리했다.

### 2026-09-14 / 수동 입력 제거와 캐릭터별 12개 유효 옵션 자동 평가

- 목적: 자동으로 가져온 유물을 다시 수동 입력하는 중복 과정을 없애고, 카스토리스 같은 HP 기반 치명타 딜러와 공격력·방어력 기반 캐릭터를 같은 `치명타 딜러` 기준으로 평가하던 문제를 해결한다.
- 조사: MiHoMo 모델에는 정규화된 부옵션 가중치가 없음을 확인했다. StarRailScore `score.json`은 캐릭터 ID별 12개 부옵션 가중치를 제공하고, 카스토리스 `1407`은 HP%·치확·치피 `1.0`, 공격력% `0`, 속도 `0.1`이다. 게임 리소스의 카스토리스 스킬도 최대 HP 계수임을 교차 확인했다. 상세 근거는 `docs/research/CHARACTER_RELIC_WEIGHTS.md`에 기록했다.
- 데이터 정책: StarRailScore 저장소의 명시적 라이선스를 확인하지 못해 전체 JSON을 프로젝트에 복제하지 않았다. 재현 가능한 결과를 위해 원본 주소를 확인한 커밋 `fb8268bc6345c52501bd4ec23f8df89b26497e0a`에 고정했다. API는 주옵션, 12개 부옵션과 양수 `max`를 검증해 프로세스 메모리에 재사용하고 원본 JSON을 Redis에 30일 캐시한다. 미등록 ID에는 임의의 범용 점수를 적용하지 않는다.
- 도메인 변경: `CharacterScoring.cs`에 캐릭터 프로필, 주·부옵션 가중치, 핵심·유효·보조·비유효 판정과 높은 가중치의 중립적 요약을 추가했다. `ScoreCalculator`는 5성 롤을 원본의 `1.0·1.1·1.2` 단위로 복원하고, 주옵션 50점과 `max`로 정규화한 부옵션 50점을 합산한다. 기존 수동 API 계산은 호환성을 위해 유지하되 화면에서는 사용하지 않는다.
- API 변경: `StarRailScoreProfileProvider`와 엄격한 JSON 파서를 추가하고 `POST /api/scores/character-batch`를 연결했다. 원본 연결 실패 때 Redis 캐시로 복구하며, 캐릭터별 자동 계산 결과도 기존 최근 20개 기록과 누적 횟수에 저장한다. Redis가 없으면 계산 결과와 명시적인 저장 경고를 함께 반환한다.
- 화면 변경: `Home.razor`에서 수동 프로필 선택, `전체 점수 계산`, `입력 적용`, 아래 `유물 정보 입력`과 별도 `감정 결과` 영역을 제거했다. 가져오기 직후 첫 캐릭터를 자동 계산하고 캐릭터 탭을 누를 때마다 해당 캐릭터 기준으로 다시 계산한다. 유형 요약 아래 12개 옵션 전체를 표시하며, 각 유물에는 주옵션과 부옵션의 유효도·가중치·기여 점수를 모두 표시한다.
- TDD: 구현 전 카스토리스 분류, 12개 옵션 보존, HP% 점수 반영과 공격력% 0점, 누락 가중치 거부, 원본 JSON 12개 필드 매핑 테스트를 작성해 컴파일 실패를 확인했다. 리뷰 후 주옵션·`max` 정규화, 프로세스당 원본 1회 조회, 원격 503 시 Redis 폴백, 최고 품질 롤 `1.2`, 이론 세팅 100점과 과대 문자열 거부 테스트까지 추가했다. 구현 후 도메인 28개와 API 14개 테스트가 통과했다.
- 실제 API 검증: 카스토리스 `max=9.24`를 적용한 +15 머리 샘플에서 고정 HP 주옵션 50점, HP%·치확·치피 최고 품질 롤이 각각 6.5점, 공격력%가 0점으로 계산되어 총 69.5점(SS)이 반환됐다. Redis 미설치 상태에서도 `200 OK`, `redisSaved=false`와 저장 경고를 함께 확인했다.
- 실제 브라우저 검증: 공개 UID `800333171`에서 카스토리스 탭의 HP%·치확·치피 `1.0`, 공격력% `0`, 속도 `0.1`과 유물 6개의 자동 점수를 확인했다. 각 카드의 주옵션에도 `핵심 · ×1.0 · +50.0점`처럼 적합도가 표시됐다. 아케론 탭은 `공격력 기반 · 치명타 중심`, 공격력%·치확·치피 `1.0`, 속도 `0.8`로 즉시 바뀌었다. `유물 정보 입력` 제목은 0개였고 브라우저 콘솔 오류·경고는 0개였다.
- 개발 중 문제와 해결: 기존 Release API 프로세스가 `ArtifactGrade.Domain.dll`을 사용 중이라 첫 Release 빌드가 파일 잠금으로 실패했다. `netstat -ano`로 5072·5111 포트 소유자가 이 저장소 프로세스임을 확인하고 두 프로세스만 종료했다. 이후 샌드박스 안에서 시작한 숨김 자식 프로세스가 셸 종료와 함께 끝나 포트를 열지 못했으므로, 동일한 Release `dotnet run --no-build` 명령만 백그라운드 실행 권한으로 시작해 최신 서버를 유지했다.
- 최종 리뷰 보완: 원본 `max`는 최고 롤을 단순히 `1.0`으로 보는 단위가 아니라 기본 롤과 품질 상승을 `1.0·1.1·1.2`로 세는 단위임을 고정 커밋의 README와 `scripts/generate.py`에서 다시 확인했다. GitHub 비인증 API가 조회 한도에 걸려 고정 커밋만 임시 폴더에 체크아웃해 생성 스크립트와 5성 `RelicSubAffixConfig.json`을 직접 대조했고, 조사 뒤 임시 복제본은 삭제했다. 5성 부옵션의 기본값·단계값 조합 중 불러온 수치와 가장 가까운 롤 구성을 복원하도록 계산을 수정했다. 공개 캐릭터 배치 API에는 1MB 본문과 문자열 길이 제한을 추가하고, Redis에는 캐릭터·배치 메타데이터를 한 번만 저장해 입력 증폭을 막았다.
- 보안 경계 실측: 정상 캐릭터 배치 요청은 200과 계산 결과를 반환했고, 101자 캐릭터 이름은 400, 1MB를 넘는 본문은 413으로 거부됐다.
- 최종 자동 검증: Release 빌드는 경고 0개·오류 0개였고 xUnit 42개(도메인 28개, API 14개), `dotnet format --verify-no-changes`, `git diff --check`, 기존 API·Scanner 스모크와 실제 UID 48개 가져오기가 모두 통과했다.
- 최종 재검토: 명세와 코드 기준 검토자가 롤 단위, 주옵션, Redis 기록·캐시, 입력 제한 보완 후 전체 변경분을 다시 확인했으며 남은 지적 사항은 없었다.
- 사용법: `start-dev.cmd`를 더블클릭하고 UID를 입력해 `UID 불러오기`를 누르거나 Scanner JSON을 선택한다. 그 뒤 캐릭터 탭만 선택하면 평가 기준과 유물 점수가 자동으로 바뀐다. 프로필 선택, 직접 수치 입력과 별도 계산 버튼은 필요 없다.
- 남은 한계: 현재 총점은 주·부옵션의 선형 가중치 휴리스틱이며 파티·광추·성혼·속도/효과 명중 목표 구간은 반영하지 않는다. 이 한계를 화면과 README에 명시했다.

### 2026-09-14 / 캐릭터 자동 평가 등급 밸런스 조정

- 목적: 주옵션 50점을 더한 뒤 대부분의 유물이 SS·SSS로 표시되어 등급의 구분력이 사라진 문제를 바로잡는다.
- 원인: 기존 `C 35 / B 35 / A 45 / S 55 / SS 65 / SSS 75` 구간은 부옵션 중심의 이전 점수용이었다. 캐릭터 자동 평가는 주옵션과 부옵션을 각각 50점으로 합산하지만 같은 등급 함수를 공유하고 있어, 올바른 +15 주옵션만으로도 이미 50점에서 시작했다.
- 실측 방법과 결과: 실행 중인 API에서 `GET /api/import/uid/800333171`로 캐릭터 8명·유물 48개를 가져오고, 캐릭터별로 `POST /api/scores/character-batch`를 호출해 전체 점수를 집계했다. 기존 분포는 SSS 39개, SS 7개, S 1개, A 1개로 SS 이상이 46/48개였다. 점수 범위는 48.2~98.7점, 중앙값은 84.2점이었다.
- 결정: 캐릭터 자동 평가 등급을 `C < 60`, `B 60`, `A 70`, `S 80`, `SS 90`, `SSS 97점 이상`으로 조정했다. 총점과 가중치 계산식은 바꾸지 않고 등급 표시만 재보정했다. 기존 수동 `POST /api/scores`와 `POST /api/scores/batch`는 호환성을 위해 종전 등급 구간을 유지한다.
- TDD 과정: 59.9·60·69.9·70·79.9·80·89.9·90·96.9·97점 경계값 테스트를 먼저 추가해 기존 구현에서 9개가 실패하는 것을 확인했다. 캐릭터 전용 등급 함수를 연결한 뒤 10개 경계값이 모두 통과했다. 테스트용 롤 값에 처음 HP 기본 롤 수치를 공격력 부옵션으로 사용해 목표 점수가 두 배 가까이 나온 문제는 5성 고정 공격력 기본 롤 `16.935019`로 바로잡았다.
- 화면과 문서: `Home.razor`의 계산 기준 상자에 새 등급 컷을 표시했다. `README.md`, `BLOG_POST_DRAFT.md`, `CHARACTER_RELIC_WEIGHTS.md`에는 현재 구간, 기존 API와의 차이, 실제 사용 분포를 반영했다.
- 리뷰와 수정: 기준 검토에서 캐릭터 등급 숫자가 계산 switch와 화면 문구에 중복돼 다음 조정 때 서로 달라질 수 있는 문제를 발견했다. `CharacterGradeCatalog`가 구간과 표시 문구를 함께 소유하고 계산기와 UI가 이를 참조하도록 바꿨다. 요구사항 검토에서 확인한 원격 미푸시는 최종 수정 커밋 뒤 처리한다.
- 재측정: 같은 48개 표본은 SSS 2개, SS 13개, S 20개, A 9개, B 3개, C 1개로 분산됐다. 특히 카스토리스 6개는 SSS 1개·SS 3개·S 2개, 에버나이트 6개는 S 5개·A 1개로 화면에서 확인했다.
- 실행·검증 명령: `dotnet build ArtifactGrade.slnx -c Release --no-restore --disable-build-servers --nologo`, `dotnet test ArtifactGrade.slnx -c Release --no-build --no-restore --disable-build-servers --nologo`, `dotnet format ArtifactGrade.slnx --verify-no-changes --no-restore --verbosity minimal`, `git diff --check`, `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/api-smoke.ps1`, `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/import-smoke.ps1 -Uid 800333171`을 실행했다.
- 최종 정리 중 문제와 해결: 캐릭터용 함수와 구분되도록 기존 등급 함수 이름을 `GetLegacyGrade`로 바꾼 직후 전체 테스트가 실행 중인 API의 DLL 잠금 때문에 실패했다. `netstat -ano`와 프로세스 경로로 5072·5111 포트 소유자가 이 저장소 서버임을 확인해 두 프로세스만 종료하고, Release 전체 빌드와 52개 테스트를 다시 통과시킨 뒤 최신 서버를 재실행했다.
- 최종 검증: Release 빌드는 경고 0개·오류 0개였고 xUnit 52개(도메인 38개, API 14개), 형식 검사, 공백 검사, 기존 API 스모크, Scanner 일괄 계산, 실제 UID 48개 가져오기가 모두 통과했다. 웹 클라이언트는 HTTP 200으로 응답했고 `/api/health`의 503은 Redis가 설치되지 않은 현재 환경에서 예상한 상태다. 최신 API와 클라이언트는 각각 5072·5111 포트에서 다시 실행했다.
- 브라우저 확인: `http://localhost:5111`에서 새 등급 안내, UID 자동 가져오기, 캐릭터 탭 전환과 카드별 등급을 확인했다. 브라우저 로그에는 Blazor 디버깅 안내 1건만 있었고 오류·경고는 없었다. 서버 교체 뒤 기존 탭 연결이 만료된 경우 새 로컬 앱 탭을 열어 최신 화면에 다시 연결했다.

### 2026-09-14 / 캐릭터 일러스트 아래 장비 종합 요약 추가

- 목적: 오른쪽 유물 카드 두 줄에 맞춰 길어진 왼쪽 캐릭터 열에서 일러스트 아래 남는 공간을 실제 장비 판단에 활용한다.
- 화면 구성 결정: 일러스트 크기를 억지로 늘리지 않고 아래에 `RELIC OVERVIEW`를 배치했다. 평균 점수와 평균 등급, 등급 분포, 세트 구성, 핵심 주옵션 개수, 교체 우선 유물과 사유, 점수가 높은 핵심 부옵션 기여 합계 3개를 표시한다. 왼쪽은 캐릭터 전체 장비 상태, 오른쪽은 유물별 상세 내용이라는 역할로 구분했다.
- 도메인 변경: `CharacterLoadoutSummary.cs`의 `CharacterLoadoutSummarizer`가 기존 캐릭터 프로필과 점수 결과를 입력으로 받아 모든 집계를 계산한다. 화면에는 계산 로직을 두지 않았고 별도 상태도 저장하지 않는다. 평균 등급은 기존 `CharacterGradeCatalog`를 재사용하며 등급 분포 순서도 같은 카탈로그가 소유한다. 교체 우선은 계산 가능한 유물 중 최저점이며, 주옵션이 핵심이 아니면 그 판정을 먼저, 핵심이면 비유효 부옵션 개수를 사유로 표시한다.
- TDD 과정: SSS 98점, SS 92점, A 72점 유물 세 개로 평균 87.3점 S, 등급 분포, 세트별 장착 수 2개+1개, 핵심 주옵션 2/3, 최저점 몸통과 `주옵션 비유효`, HP% 20점·치명타 확률 10점 합계를 기대하는 테스트를 먼저 추가했다. 요약 타입이 없어 컴파일 실패하는 것을 확인한 뒤 최소 구현을 추가했고, 유효 점수가 하나도 없으면 요약을 만들지 않는 테스트까지 2개가 통과했다.
- 화면과 반응형: `Home.razor`는 캐릭터 이미지 영역을 `character-art`로 분리하고 그 아래에 도메인 요약을 표시한다. `Home.razor.css`는 요약이 데스크톱에서 오른쪽 카드 높이의 남는 부분을 채우게 했고, 640px 이하에서는 이미지 360px 다음에 요약과 유물 카드가 한 열로 이어지도록 했다. 430px 이하에서는 세트와 핵심 주옵션 칸도 한 열로 바뀐다.
- 실제 사용: 공개 UID `800333171`의 48개 유물로 확인했다. 에버나이트는 평균 82.1점 S, S 5개·A 1개, 구세주 유물 4개+습골지 유물 2개, 핵심 주옵션 6/6, 연결 매듭 73.2점과 `비유효 부옵션 1개`가 표시됐다. 카스토리스 탭으로 바꾸면 평균 91.5점 SS, SSS 1개·SS 3개·S 2개, 연결 매듭 85.2점과 `비유효 부옵션 2개`, 치명타 피해 113.1점·치명타 확률 79.0점·HP% 52.5점 합계로 즉시 갱신됐다.
- 검증 중 문제와 해결: 첫 UID 조회는 공개 프로필 서비스 연결 실패 안내를 반환했다. 같은 화면에서 잠시 뒤 한 번 재시도해 캐릭터 8명·유물 48개를 정상 수신했고, 외부 서비스의 일시적 실패가 기존 오류 안내로 드러나는 것도 함께 확인했다. 전체 페이지 캡처는 브라우저 도구에서 반복 렌더링됐지만, 요약 영역을 직접 눌러 화면 중앙으로 이동한 뒤 캡처해 왼쪽 요약과 오른쪽 유물 두 번째 줄의 높이·간격을 확인했다.
- 리뷰와 수정: 세트별 장착 유물 수를 화면에서 `4세트`처럼 표시하면 활성 세트 효과 수로 오해할 수 있다는 지적을 반영했다. 도메인 필드를 `Count`에서 `PieceCount`로 명확히 바꾸고 화면도 `4개`·`2개`로 표시하도록 수정했으며, 테스트와 블로그의 표현도 같은 의미로 통일했다.
- 문서 변경: `README.md`의 주요 기능과 UID 사용법, `BLOG_POST_DRAFT.md`의 구현 기능·문제 해결·실사용 후기에 종합 요약 사용법과 실제 결과를 추가했다.
- 자동 검증: Release 빌드는 경고 0개·오류 0개였고 xUnit 54개(도메인 40개, API 14개), `dotnet format --verify-no-changes`, `git diff --check`, 기존 API 스모크, Scanner 일괄 계산과 UID 48개 가져오기가 모두 통과했다. 브라우저 로그에는 Blazor 디버깅 안내만 있었고 오류·경고는 없었다.
- 사용법: UID 또는 Scanner JSON을 불러오고 캐릭터 탭을 선택한다. 왼쪽 일러스트 아래에서 장비 전체 상태와 교체 우선을 먼저 보고, 오른쪽 카드에서 해당 유물의 주옵션·부옵션과 기여 점수를 확인한다.
- 해석 제한: `교체 우선`은 현재 장착한 유물 중 상대적으로 점수가 가장 낮은 항목을 알려줄 뿐, 인벤토리의 다른 유물과 실제 교체 결과를 시뮬레이션하지 않는다.

### 2026-09-15 / 캐릭터·유물 스탯 가독성 개선

- 목적: 캐릭터 가중치와 유물 카드에 6.5~10px 글자가 몰려 있어 스탯 이름, 유효도, 가중치와 기여 점수를 한눈에 읽기 어려운 문제를 개선한다. 첫 화면의 핵심 제목은 시선을 지나치게 차지하지 않도록 소폭 축소한다.
- 기준 확인: 변경 전 824px 화면에서 제목은 41.2px, 캐릭터 옵션명은 8px, 유효도는 7px, 유물 이름은 10px, 부옵션 수치는 7.5px, 부옵션 유효도·가중치는 6.5px로 계산됐다. 변경 전 전체 xUnit 54개와 Client Debug 빌드가 통과해 기준 상태가 정상임을 확인했다.
- CSS 변경: `Home.razor.css`에서 캐릭터 옵션명·유효도·가중치, 장비 종합 요약, 유물 이름·세트·주옵션·부옵션·점수를 단계별로 키웠다. 스탯 핵심 정보는 최소 10px 이상, 옵션명과 점수는 주로 11~15px로 조정했다. 최상단 제목은 `clamp(35px, 5vw, 59px)`에서 `clamp(33px, 4.5vw, 54px)`로 줄였고 모바일 고정값도 36px에서 33px로 조정했다.
- 반응형 보완: 390px 화면에서 `격파 특수효과%` 하나가 3열 카드 폭을 넘어 말줄임되는 것을 발견했다. 430px 이하에서 캐릭터 옵션 표를 2열로 바꿔 확대된 글자를 유지하면서 긴 옵션명도 온전히 표시하도록 했다.
- 검증 방식: 계산 로직이나 Razor 구조는 바꾸지 않은 표시 전용 변경이므로 새 도메인 단위 테스트 대신 Client 빌드와 브라우저의 실제 계산 스타일·가로 넘침 여부를 공개 UID 화면에서 확인한다. 변경 후 824px 화면의 제목은 37.08px, 캐릭터 옵션명은 11px, 유효도는 10px, 유물 이름·점수는 13px, 주옵션 값은 14px, 부옵션 수치·기여도는 11px가 됐다.
- 실제 화면 검증: 공개 UID `800333171`의 캐릭터 8명·유물 48개를 최신 Release 화면에서 불러왔다. 824px 화면에서 캐릭터 옵션명, 유물 이름·세트·주옵션·부옵션의 가로 잘림은 모두 0건이었다. 390px 화면은 옵션 표가 2열, 유물 카드가 1열로 표시됐고 같은 항목의 잘림 0건, 문서 너비 375px·스크롤 너비 375px로 가로 넘침도 없었다. 브라우저 오류·경고 로그는 0건이었다.
- 최종 자동 검증과 실행: 전체 Release 빌드는 경고 0개·오류 0개, xUnit 54개(도메인 40개, API 14개), `dotnet format --verify-no-changes`, `git diff --check`가 통과했다. 최신 API와 클라이언트를 5072·5111 포트에 다시 실행했고 클라이언트는 HTTP 200으로 응답했다. `/api/health`의 503은 Redis가 설치되지 않은 현재 환경에서 예상한 상태다.

### 2026-09-15 / 상단 제목 고아 줄 수정

- 증상과 재현: 1440px 화면에서 제목 영역은 최대 760px, 글자는 54px이었다. 문자별 렌더링 위치를 측정하자 첫 줄 `이 유물, 이 캐릭터에게 정말 좋은 걸`은 742px인데 둘째 줄은 `까?` 72px뿐이어서 마지막 줄 비율이 0.10으로 실패했다.
- 원인: 제목 폭보다 전체 문장이 조금 길고 한국어 기본 줄바꿈이 글자 사이에서도 허용되어, 브라우저가 마지막 두 글자만 다음 줄로 보냈다. 968px에서는 오른쪽 계산 기준 칸 때문에 제목 열이 더 좁아져 같은 유형의 불균형이 재현됐다.
- 첫 수정과 추가 발견: `.hero h1`에 `text-wrap: balance`만 적용하면 줄 너비는 425px·389px으로 균형을 찾았지만 `캐릭터에 / 게`처럼 단어 내부가 갈라졌다. 고아 줄 비율만 확인하지 않고 실제 줄별 문자열도 함께 검사해 이 문제를 발견했다.
- 최종 수정: `text-wrap: balance`와 `word-break: keep-all`을 함께 적용했다. 글자 크기, 제목 최대 폭, 오른쪽 계산 기준 영역은 건드리지 않고 줄바꿈 책임만 제목 CSS에 추가했다.
- 실제 화면 결과: 1440px에서는 `이 유물, 이 캐릭터에게` 472px과 `정말 좋은 걸까?` 330px으로 나뉘어 마지막 줄 비율이 0.70이 됐다. 968px과 390px에서도 같은 두 문장 단위로 나뉘었고, 390px 문서 너비와 스크롤 너비가 모두 375px이라 가로 넘침이 없었다.
- 최종 검증과 실행: 전체 Release 빌드는 경고 0개·오류 0개였고 xUnit 54개(도메인 40개, API 14개), `dotnet format --verify-no-changes`, `git diff --check`가 통과했다. 최신 API와 클라이언트를 5072·5111 포트에 다시 실행했고 웹 화면은 HTTP 200으로 응답했다. `/api/health`의 503은 Redis 미설치 환경에서 예상한 상태다.

### 2026-09-15 / Docker 없는 외부 배포 준비와 제출 자료 정리

- 목적: 로컬 Docker가 없는 현재 환경에서도 화면·API·Redis를 외부에 배포할 수 있게 하고, 실제 사용 후기와 제출용 캡처를 준비한다.
- 공식 문서 조사: Render는 저장소의 Dockerfile을 원격 빌드하고 Blueprint에서 웹 서비스와 Key Value를 함께 정의할 수 있다. 무료 웹 서비스와 무료 Key Value를 제공하지만 웹 서비스는 15분 동안 요청이 없으면 정지하며, 무료 Key Value는 재시작 시 데이터가 유지되지 않는다. 배포 대상은 한국에서 가까운 싱가포르 리전과 무료 플랜으로 고정했다.
- 배포 구성: `Dockerfile.Render`가 Blazor WebAssembly 결과물을 ASP.NET Core API의 `wwwroot`에 합쳐 한 웹 서비스와 한 주소로 제공한다. `render.yaml`은 웹 서비스와 외부 접근을 차단한 Key Value를 생성하고 내부 `redis://` 연결 주소를 `ConnectionStrings__Redis`에 전달한다. 관리형 Redis URL과 기존 StackExchange.Redis 형식을 모두 읽도록 주소 변환 코드를 추가했다.
- TDD: `redis://`, `rediss://` 인증 주소, 기존 StackExchange.Redis 문자열을 다루는 테스트 3개를 먼저 컴파일 실패 상태로 확인했다. 최소 변환 구현 후 3개가 통과했고 API 테스트는 14개에서 17개로 늘었다. GitHub Actions도 도메인 테스트만이 아니라 솔루션 전체 테스트를 실행하도록 수정했다.
- 통합 실행 문제와 해결: Dockerfile과 같은 폴더 구조를 `.tmp/render-smoke`에 만들고 임시 포트 5080에서 실행했다. 루트 HTML은 200이었지만 ASP.NET Core 기본 정적 파일 제공기가 Blazor 한국어 ICU `.dat`를 제공하지 않아 실제 브라우저가 97%에서 멈췄다. `.dat`의 미디어 형식을 `application/octet-stream`으로 등록한 뒤 ICU 파일, Blazor 런타임, 화면과 API가 같은 주소에서 정상 응답했다.
- 실사용 및 캡처: 통합 실행 화면에서 공개 UID `800333171`의 캐릭터 8명·유물 48개를 불러왔다. 데스크톱 결과와 390px 모바일 화면에서 계산과 이미지가 정상 표시됐고 가로 넘침 및 브라우저 오류는 0건이었다. `docs/images`에 서비스 개요, 데스크톱 결과, 모바일 개요와 모바일 캐릭터 요약 캡처를 저장해 README와 블로그 초안에 연결했다.
- 기준 빌드 문제와 해결: 처음 Release 빌드는 실행 중인 API가 출력 DLL을 잠가 실패했다. 해당 API만 잠시 중지한 뒤 빌드와 테스트를 통과시키고 숨김 프로세스로 다시 실행했다. 첫 숨김 실행은 샌드박스 환경의 `Path/PATH` 중복 때문에 실패했으나 승인된 외부 실행 환경에서 재기동해 복구했다.
- 배포 검토와 수정: 첫 검토에서 Render의 상태 검사가 Redis 의존 `/api/health`를 사용해 Redis 재시작 때 계산 가능한 웹 서비스까지 재시작할 수 있고, SPA 폴백 때문에 존재하지 않는 `/api/*`가 HTML 200으로 위장되는 문제를 확인했다. Redis와 무관한 `/api/health/live`를 Render 상태 검사로 지정하고, 잘못된 API 주소는 JSON 404로 끝나게 분리했다. 생존 상태, API 404, SPA 클라이언트 경로의 세 회귀 테스트를 먼저 실패시킨 뒤 수정해 통과시켰다.
- 최종 자동 검증: 전체 Release 빌드는 경고 0개·오류 0개였고 xUnit 60개(도메인 40개, API 20개), `dotnet format --verify-no-changes`, `git diff --check`가 통과했다. 통합 배포 형태에서 `/api/health/live`는 200 JSON, 존재하지 않는 API는 404 JSON, 임의 클라이언트 경로와 Blazor 런타임 파일은 200을 반환했다. Redis가 없는 임시 검증 환경의 `/api/health`는 예상대로 503을 반환했다.
- GitHub 반영: 최종 기능 커밋 `7e19f78`을 `codex/csharp-relic-scorer`와 `main`에 fast-forward로 푸시했다. GitHub Actions CI 실행 #18에서 솔루션 전체 테스트와 Release 빌드가 성공했다.
- 현재 상태: 실제 Render 서비스와 Key Value 생성은 Render 로그인, GitHub 저장소 연결, 무료 리소스 생성 승인이 필요한 외부 작업이므로 Blueprint와 사용법까지만 자동 준비했다. 배포 후 `/api/health`의 `healthy/connected`, UID 두 번째 조회의 캐시 적중과 외부 주소를 문서에 기록해야 평가 항목 2가 완료된다. 실제 블로그 또는 SNS 게시 URL이 생기기 전까지 평가 항목 5는 완료로 표시하지 않는다.
